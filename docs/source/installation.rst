============
Installation
============

At the command line::

    $ pip install gunc

Or using conda::
    $ conda install gunc

The gunc database also needs to be downloaded::

    $ wget url_tbd -o path
